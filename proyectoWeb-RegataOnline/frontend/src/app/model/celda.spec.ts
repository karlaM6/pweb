import { TestBed } from '@angular/core/testing';

import { Celda } from './celda';

describe('Celda', () => {
  it('should create an instance', () => {
    expect(new Celda()).toBeTruthy();
  });
});
