import { TestBed } from '@angular/core/testing';

import { Modelo } from './modelo';

describe('Modelo', () => {
  it('should create an instance', () => {
    expect(new Modelo()).toBeTruthy();
  });
});
